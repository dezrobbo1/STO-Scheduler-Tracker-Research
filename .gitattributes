# The conformance corpus is pinned by SHA-256 to an upstream commit: stored and
# checked out byte for byte, and exempt from the whitespace check for that reason.
src/sto/conformance/corpus/** -text -whitespace
